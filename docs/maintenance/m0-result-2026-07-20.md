# M0 result: 2026-07-20

> Status: Evidence snapshot
> Owns: Sanitized result of the 2026-07-20 M0 clean-profile walkthrough at the pinned baseline revision
> Update when: Never; add a new dated result for a later execution
> Last verified: 2026-07-20

This record preserves one execution of the normative
[M0 clean-profile walkthrough](m0-clean-profile-walkthrough.md). It is evidence for the tested
revision, not a current compatibility claim.

## Environment

- Revision: `929773467630c7b7ac082c23c15cec64d3b20743`
- Package version: `0.1.0`
- Platform: macOS 26.5.2 (25F84), Apple silicon
- Signing: local development identity; not notarized

## Result

**Passed.**

- `npm run check` passed 67 Node/UI tests, the Svelte production build, documentation validation,
  and `cargo check`.
- `cargo test --manifest-path src-tauri/Cargo.toml` passed all 48 Rust tests.
- The signed `.app` built successfully and passed strict deep `codesign` verification.
- An empty temporary `CODEX_HOME` displayed first-run, disconnected, no-pet, import, reveal, and
  disabled-removal guidance without reading the operator's installed pets.
- A wrapped ZIP previewed a sanitized V1, 3072 x 3744, 2x package, installed successfully, and
  left no staging directory.
- Selecting the source folder detected the ID conflict, displayed a native replacement
  confirmation, and completed the atomic replacement.
- Relaunching the packaged app with the same temporary profile rediscovered and rendered the
  installed pet.
- Rescanning a deliberately invalid manual folder kept the valid pet selected and exposed the
  folder's manifest error under **Needs attention**.
- Confirmed removal returned the catalog to empty, disabled removal, restored import guidance, and
  cleared the rendered pet.
- The same packaged build, relaunched against the normal profile, attached to the selected live
  Codex task and reported Working. No conversation text or private payload was retained as evidence.

The temporary smoke profile was isolated from the operator's normal Codex profile and moved to
Trash after the run. Clean-profile package management and normal-profile live attachment were
verified as consecutive scenarios because the empty profile intentionally had no Codex discovery
data. Public-download trust remained outside this result because the bundle was not notarized.
